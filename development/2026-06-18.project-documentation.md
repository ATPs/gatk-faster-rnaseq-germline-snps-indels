# Project Documentation

## Aim

Add the missing top-level project documentation in English so the repository has:

- a user-facing `README.md`
- a `design.md` that explains the current architecture and every tool in the repo
- an explicit MIT `LICENSE`

## Changes

- Added `README.md` with:
  - project purpose
  - current status
  - repository layout
  - build pointer
  - quick-start examples for FASTQ and aligned BAM inputs
  - backend controls
  - resume/debug workflow notes
  - output layout
- Added `design.md` with:
  - architecture overview
  - pipeline data flow
  - execution model
  - output layout
  - explanation of external tools
  - explanation of Python pipeline tools
  - explanation of Rust pipeline binaries
  - explanation of Rust HaplotypeCaller validation tools
  - explanation of internal Rust library modules
  - current limitations
- Added `LICENSE` with the MIT license and copyright for Xiaolong Cao.

## Verification

- Confirmed the documented top-level entrypoint matches `gatk-faster-rnaseq-germline-snps-indels.py`.
- Confirmed the documented Python step tools match `src/gatk_faster_rnaseq/steps/*.py`.
- Confirmed the documented Rust binaries match `src/rust/Cargo.toml`.
- Confirmed the documentation reflects the current post-refactor layout under `src/gatk_faster_rnaseq/` and `src/rust/`.

## Conclusion

The repository now has the minimum project documentation needed for contributors and users to understand what the pipeline does, how to run it, how the code is organized, and what each internal tool is for.
