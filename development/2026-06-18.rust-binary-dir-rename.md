# Rust Binary Directory Rename

## Aim

Rename the installed Rust executable directory from `src/rust/bin` to `rust_binary` so it is clearer that this folder contains built pipeline artifacts rather than Cargo source entrypoints.

## Changes

- Renamed the installed executable folder:
  - from `src/rust/bin`
  - to `rust_binary`
- Updated the Python pipeline defaults to resolve Rust tools from the new folder:
  - `src/gatk_faster_rnaseq/pipeline/runner.py`
  - `src/gatk_faster_rnaseq/steps/common.py`
- Updated active project guidance and build documentation:
  - `AGENTS.md`
  - `build.md`
  - `development/2026-06-18.server-specific-build.md`
  - `development/2026-06-18.build-guide-generalization.md`

## Verification

- `python -m py_compile gatk-faster-rnaseq-germline-snps-indels.py src/gatk_faster_rnaseq/**/*.py`
- `python gatk-faster-rnaseq-germline-snps-indels.py --help`
- Confirmed binaries are present under repo-root `rust_binary/`
- Ran a shortened real-data smoke using the default Rust binary lookup path and confirmed completion:
  - output VCF:
    `/XCLabServer002_fastIO/gatk-faster-rnaseq/refactor_smoke_20260618_rust_binary_root/rust.intervals-rust.splitncigar-rust.baserecal-rust.applybqsr-rust.skip-star-markdup-splitncigar-baserecal-hcpf-hc/SRR949115.filtered.vcf.gz`

## Conclusion

Repo-root `rust_binary/` is a clearer installed-artifact directory name than `src/rust/bin`, and the pipeline now resolves Rust executables from that location by default.
